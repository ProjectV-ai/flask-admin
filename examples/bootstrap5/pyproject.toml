[project]
name = "example-bootstrap5"
version = "0.1.0"
description = "Bootstrap5 Customization Example."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask-admin[sqlalchemy-with-utils]",
    "flask-babel>=4.0.0",
]

[tool.uv.sources]
flask-admin = { path = "../../", editable = true }
