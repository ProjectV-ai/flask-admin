[project]
name = "example-forms-files-images"
version = "0.1.0"
description = "Forms Files and Images Example."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask-admin[sqlalchemy-with-utils,images]",
    "flask-babel>=4.0.0",
    "redis==4.0.0",
]

[tool.uv.sources]
flask-admin = { path = "../../", editable = true }
